package org.projectcontinuum.core.cloud.gateway.config

// CORS is handled by the backend services (api-server, credentials-server, etc.)
// The gateway simply forwards CORS headers from the backend responses.
// No additional CORS configuration is needed here.
